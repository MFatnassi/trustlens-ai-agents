# Treat these as binary — never apply line-ending (CRLF/LF) conversion,
# which would corrupt the files.
*.pdf   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.ico   binary
*.gif   binary
*.webp  binary
*.woff  binary
*.woff2 binary
